import { LessTabHeaderDirective } from './less-tab-header.directive';

describe('LessTabHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new LessTabHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
